case "menu": {
    // melda.sendMessage(m.chat, { react: { text: `${randomemoji}`, key: m.key }})
    // addCountCmd('#menu', m.sender, _cmd)
    // const tampilan = `Perkenalkan nama saya "Fauzialifatah" Saya adalah pembuatan base script bot ini, jika kalian suka jangan lupa join saluran saya terimakasih
    
    // ◈ ${prefix}alifatah
    // ◈ ${prefix}delsampah`
    // melda.sendMessage(m.chat, {
    // document: fs.readFileSync("./package.json"),
    // fileName: `Thezy.StartupX`,
    // fileLength: 99999999999999,
    // pageCount: "100",
    // mimetype: 'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
    // caption: tampilan,
    // contextInfo: {
    // forwardingScore: 10,
    // isForwarded: true,
    // mentionedJid: [m.sender],
    // businessMessageForwardInfo: {
    // businessOwnerJid: botNumber
    // },
    // forwardedNewsletterMessageInfo: {
    // newsletterJid: `120363367787013309@newsletter`,
    // serverMessageId: null,
    // newsletterName: 'Hai saya menggunakan WhatsApp!'
    // }
    // }
    // }, {});
    // }
    // break
    
    // case "alifatah":
    // melda.sendMessage(m.chat, { react: { text: `${randomemoji}`, key: m.key }})
    // const uploadFile = { upload: melda.waUploadToServer };
    // var imageMessage = await prepareWAMessageMedia(
    //  {
    // image: { url: `https://files.catbox.moe/iejoer.jpg` },
    //  },
    //  uploadFile,
    // );
    // const product = {
    //  productImage: imageMessage.imageMessage,
    //  productId: "8860841927332916",
    //  title: "PROJECT BY ALIFATAH - MD",
    //  description: "Mau Nyari Apa Bang?",
    //  currencyCode: "IDR",
    //  priceAmount1000: "50000000",
    //  productImageCount: 1,
    // };
    // const productData = {
    //  product: product,
    //  businessOwnerJid: global.ownNumb + "@s.whatsapp.net",
    // };
    // const productMessage = { productMessage: productData };
    // var response = await generateWAMessageFromContent(
    //  m.chat,
    //  proto.Message.fromObject(productMessage),
    //  m.quoted && m.quoted.fromMe
    // ? { contextInfo: { ...m.msg.contextInfo } }
    // : {},
    // );
    // await melda.relayMessage(m.chat, response.message, {
    //  messageId: response.key.id,
    // });
    // break;